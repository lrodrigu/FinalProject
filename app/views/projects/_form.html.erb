<%= form_for @project do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %> <br>
	<%= f.collection_select(:investor_id, Investor.all, :id, :full_name, prompt: true) %>

	<%= f.submit %>
<% end %>

